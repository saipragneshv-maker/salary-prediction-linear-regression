# -*- coding: utf-8 -*-
"""Project 2.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1-FHHtzLHCWhIX-jcAWFaTovI_Wpm_w3J
"""

import pandas as pd

import numpy as np

import matplotlib.pyplot as plt

import seaborn as sns

se = pd.read_csv("/content/sample_data/01_salary_vs_experience_clean.csv")

se.head()

se.tail()

se.dtypes

se.isna().sum()

se.describe()

se.info()

se.shape

se.duplicated().sum()

se["years_experience"].unique()
#print(se["years_experience"].nunique())



count = se["years_experience"].value_counts()
print(count)

plt.bar(count.index,count.values)
plt.show()

plt.scatter (se['years_experience'],se['annual_salary_usd'],color = 'black')
plt.show()

sns.regplot(x = 'years_experience',y = 'annual_salary_usd',data = se)

sort_experience = se.sort_values('years_experience',ascending = False)

plt.plot(sort_experience['years_experience'],se['annual_salary_usd'],data = se)
plt.show()

plt.bar(sort_experience['years_experience'],se['annual_salary_usd'])
plt.show()

salary_by_experience = se.groupby('years_experience')['annual_salary_usd'].sum()
salary_by_experience

plt.pie(salary_by_experience,labels = salary_by_experience.index,autopct = '%1.1f%%')
plt.show()

se["years_experience"].unique()

junior = se[(se["years_experience"] >= 0.3) &
            (se["years_experience"] < 5.3)]

mediocre = se[(se["years_experience"] >= 5.3) &
              (se["years_experience"] < 10.3)]

senior = se[se["years_experience"] >= 10.3]
print(se)

plt.pie([junior.shape[0],mediocre.shape[0],senior.shape[0]],
       labels = ['junior','mediocre','senior'],
       autopct = '%1.1f%%')
plt.show()

x = se['years_experience']
y = se['annual_salary_usd']

from sklearn.model_selection import train_test_split
x_train,x_test,y_train,y_test = train_test_split(x,y,test_size = 0.2,random_state = 42)

from sklearn.linear_model import LinearRegression
model = LinearRegression()
model.fit(x_train.values.reshape(-1, 1),y_train)
y_pred = model.predict(x_test.values.reshape(-1, 1))
print("predicted values:",y_pred)

from sklearn.metrics import mean_squared_error,mean_absolute_error
mse = mean_squared_error(y_test,y_pred)
mae = mean_squared_error(y_test,y_pred)
rmse = np.sqrt(mse)
print("mean squared error:",mse)
print("mean absolute error:",mae)
print("root mean squared error:",rmse)

from sklearn.metrics import r2_score
r2 = r2_score(y_test,y_pred)
print("r2 score:",r2)

# The salary prediction model is 94.9 % accuarate